<Style xmlns="https://github.com/avaloniaui" Selector="Window">
  <Setter Property="Background" Value="{ThemeResource ThemeBackgroundBrush}"/>  
  <Setter Property="TransparencyBackgroundFallback" Value="{ThemeResource HighlightForegroundColor}" />
  <Setter Property="Foreground" Value="{ThemeResource ThemeForegroundBrush}"/>
  <Setter Property="FontSize" Value="{ThemeResource FontSizeNormal}"/>  
  <Setter Property="Template">
    <ControlTemplate>
      <Panel>
        <Border Name="PART_TransparencyFallback" IsHitTestVisible="False" />
        <Border Background="{TemplateBinding Background}" IsHitTestVisible="False" />
        <Panel Background="Transparent" Margin="{TemplateBinding WindowDecorationMargin}" />
        <VisualLayerManager>
          <VisualLayerManager.ChromeOverlayLayer>
            <TitleBar />
          </VisualLayerManager.ChromeOverlayLayer>
          <ContentPresenter Name="PART_ContentPresenter"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            Content="{TemplateBinding Content}"
                            Margin="{TemplateBinding Padding}"
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
        </VisualLayerManager>
      </Panel>
    </ControlTemplate>
  </Setter>
</Style>
